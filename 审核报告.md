# ImageGeoTagger v3.1.2 全面审核报告

审核日期：2026-08-05
审核范围：全部源码（models / services / ui / utils / config / main）、多语言资源、未提交改动（自 `5ae77b9` 起 23 个文件，+495/-289）、打包配置、依赖环境

---

## 一、结论摘要

| 项目 | 结果 |
|---|---|
| 语法编译 | ✅ `python -m compileall` 全量通过 |
| 单元/链路验证 | ✅ 6 项全通过（详见第二节） |
| GUI 冒烟 | ✅ MainWindow / ResultsWindow / 4 标签页创建成功 |
| 多语言完整性 | ✅ 6 语言各 571 键，无缺失/多余 |
| 依赖环境 | ✅ exifread 3.5.1、Pillow 12.2.0、tkinterdnd2、defusedxml 就绪 |
| ExifTool | ✅ `exiftool\exiftool.exe` 13.59 可用（根目录 wrapper 不可用，勿用） |
| 未提交改动 | ✅ 全部为 bug 修复/健壮性加固/清理，未发现回归 |
| **阻塞问题** | **无** |

---

## 二、实测验证记录

### 2.1 单元验证
- `parse_datetime_from_filename`：新增截图模式 `Screenshot_20240115-103000.png` → `2024-01-15 10:30:00`；`IMG_YYYYMMDD_HHMMSS`、时间在前等旧格式均正常
- `to_degrees`、`format_gps_coord`：`35.420289999999994 → "35.42029"`，负值（南纬/西经）正确
- `_parse_video_time_detailed`：`Z` 后缀 / `+08:00` 偏移 / 无时区裸时间三种输入均正确
- `csv_safe`：CSV 公式注入防护正常
- 中文 i18n 翻译加载正常；系统 codepage = 936

### 2.2 GPS 全链路 E2E（`igt_e2e_test.py`）— E2E OK
扫描（jpg+GPX 各 1）→ `process_location_info` 匹配 1 个文件 → GPS 写入磁盘（piexif 回读验证）→ 导出 GPX/CSV/JSON 成功 → CSV 含 UTF-8 BOM → 导出 GPX 可被重新解析。

### 2.3 日期处理 E2E（`igt_date_test.py`）— DATE E2E OK
- `skip_existing=True`：已有 EXIF 日期（2020-01-01）的文件保持不变
- 无日期文件（BMP，文件名亦无法解析）→ 正确跳过
- `clear_file_shooting_date` 后回读 → `0001-01-01 00:00:00`（fallback_to_fs=False）
- 写入后回读 → `2024-01-15 10:30:00`

### 2.4 GUI 冒烟
- 必须用 `TkinterDnD.Tk()` 创建根窗口（`tk.Tk()` 会报 `tkdnd::drop_target` 错误）
- MainWindow、ResultsWindow 创建成功，结果窗口 4 个标签页正常

### 2.5 两个无害现象（已定位，非 bug）
1. **"File format not recognized."**：exifread 库对 BMP 等格式直接向 stderr 打印的噪音，不影响任何读写结果（已隔离验证）
2. **`FileName encoding must be specified`**：exiftool 对含中文路径的警告；应用内部已用 `-charset filename=cp936` 重试处理，写盘成功（实测 rc=0，`1 image files updated`）

---

## 三、未提交改动审核明细（23 文件）

### 3.1 Bug 修复
| 文件 | 修复内容 |
|---|---|
| services/geo_processor.py | ① `updated_count` 双重计数导致统计虚高 → 改为仅在文件实际移入 a_list 时计数；② 坐标回填改用 `moved_files`（含处理期间被编辑对话框移出 b_list 的文件），保证对象与磁盘状态一致 |
| utils/exif_utils.py | ① QuickTime 时区**二次转换** bug：原逻辑对已带 Z/±HH:MM 的时间再按 UTC 转一次 → 新增 `_parse_video_time_detailed` 返回 `had_tz` 标志，仅无时区信息时才做 UTC 转换；② 非零退出码不再被误判为写入成功，直接抛 `_PoolExecutionError` |
| ui/date_tab.py | 批量重命名误用树列占位文本作为目标名 → 改为基于 `original_date` 重新计算真实目标名 |
| services/export_service.py | TXT 导出 `f", "` 拼接错误 → 修复为 `", "` |
| ui/geo_tab.py | 移除失效的 `ensure_exiftool_available`（死代码） |

### 3.2 健壮性/竞态加固
| 文件 | 内容 |
|---|---|
| utils/exif_utils.py | ExifTool 进程池增加 `_in_use` 计数：`close()` 时若有命令在途返回 False，由空闲回收计时器延后重试，避免进程被杀/孤儿进程；视频 GPS JSON 解析加固（空输出/非 list/损坏 → 返回 None 不崩溃）；Altitude 除零防御 |
| utils/platform_utils.py | 新增 `_thread_is_alive`（is_alive 抛 RuntimeError 时安全兜底），main_window / geo_tab / dialogs 统一复用 |
| ui/dialogs.py | 所有磁盘写入（GPS 保存/清除、日期清除）移入后台线程 `_async_apply_to_file`，错误经 `post_to_ui` 回到主线程提示并释放互斥锁 —— 编辑对话框不再冻结 |
| ui/main_window.py | 退出时检查后台线程改用安全包装；窗口配置写入 `~/.imagegeotagger`（避免打包版写 _MEIPASS 临时目录） |

### 3.3 性能优化
| 文件 | 内容 |
|---|---|
| utils/exif_utils.py + services/media_scanner.py | 新增 `extract_exif_metadata` 一次解析 EXIF 同时提取日期+GPS，扫描阶段对同一文件不再重复打开解析 |

### 3.4 状态保留（用户体验）
| 文件 | 内容 |
|---|---|
| ui/date_tab.py | 重置日期后保留 MANUALLY_RENAMED / MANUALLY_EDITED 状态 |

### 3.5 清理
- 移除未用导入：`models/gps_data.py`、`services/date_processor.py`、`utils/gpx_utils.py`、`utils/i18n.py`、`utils/media_utils.py`、`ui/main_window.py`、`ui/results_window.py`、`ui/dialogs.py` 等
- `utils/media_utils.py`：删除 `traceback.print_exc()` 噪音
- 格式化：locales 6 文件各 9 行微调（LF→CRLF 警告无害），键数不变（571）

---

## 四、遗留事项（非阻塞）

1. **22 个文件的改动尚未提交**（最后一次提交 `5ae77b9 v3.1.2`）——建议审核通过后提交，建议 commit message 概括：并发统计修复、QuickTime 时区修复、对话框异步写盘、ExifTool 进程池安全回收
2. 根目录 `exiftool.exe` 是损坏的 wrapper（找不到 `exiftool_files\perl5*.dll`），仅 `exiftool\exiftool.exe` 可用 —— 若打包脚本引用根目录版本会失败，需确认 spec 中引用的是子目录版本（spec 已配置 exiftool/ 递归打包）
3. 测试用临时脚本保留在 `C:\Users\幻想空间\AppData\Local\Temp\opencode\`（igt_e2e_test.py / igt_date_test.py），未污染项目目录；审核期间生成的 err.txt/out.txt 已清理

---

## 五、审核期间执行的关键命令

```powershell
python -m compileall -q models services ui utils main.py config.py __main__.py
python "C:\Users\幻想空间\AppData\Local\Temp\opencode\igt_e2e_test.py"   # E2E OK
python "C:\Users\幻想空间\AppData\Local\Temp\opencode\igt_date_test.py"  # DATE E2E OK
.\exiftool\exiftool.exe -ver   # 13.59
git diff --stat                # 23 files, +495/-289
```

---

## 六、二轮深度审核：新发现问题（2026-08-05 下午）

对全量代码（含已提交基线）的深入审查，**又发现并修复 5 处问题**：

| # | 严重度 | 问题 | 复现/确认方式 | 状态 |
|---|---|---|---|---|
| 1 | 中 | **TXT 导出 GPX 轨迹点时间全部显示"未知时间"**：`export_to_txt` 用 `_get_val(point, 'datetime')` 取值，但扫描产物是 `GpsPoint` 对象，其时间字段名为 `timestamp`（仅 dict 分支正常） | 实测复现 | ✅ 已修复 |
| 2 | 中 | **统计报告 GPX 时间跨度缺失**：`generate_statistics` 同根因，`_get_val(p, 'datetime')` 对 GpsPoint 全部过滤 → "轨迹点总数: 1" 但"时间跨度"两行缺失 | 实测复现 | ✅ 已修复 |
| 3 | 中 | **大视频写入固定 60s 超时不足**：池模式看门狗/独立模式均为 60s，数 GB 视频在慢盘重写元数据可能超时被误杀 → 触发备份恢复+报错；且 `update_video_gps` 5 种方法串联，最坏 15 分钟 | 代码复核 | ✅ 已修复 |
| 4 | 高 | **`_reject_unsafe_xml` O(n²) 时间复杂度假面**：`buf += chunk` 从不截断，多注释大 GPX 下复制成本平方增长。实测 5×3MB=0.7s → 60×3MB=**49.4s**，300MB 前导直接超时 2 分钟+、内存线性膨胀（合法大 GPX 也会被卡） | 实测复现 | ✅ 已修复 |
| 5 | 低 | **`_StayOpenWorker._stderr_lines` 无界增长**：长会话大量 Warning 时内存线性增长（仅重启清空） | 代码复核 | ✅ 已修复 |
| 6 | 低 | cp936 重试分支：重试后含其它真实错误也统一误报"文件名包含非ASCII字符" | 代码复核 | ✅ 已修复 |

## 七、修复内容与验证（二轮）

### 修复明细
- **1/2（export_service.py）**：`_get_val` 对无 `datetime` 属性但有 `timestamp` 的对象（GpsPoint）回退读取 `timestamp`，一处根因修复 TXT 导出与统计两处表现
- **3（exif_utils.py）**：新增 `_write_timeout_for(file_path)` 按文件大小估算超时（≤256MB 用 60s，之后按 ~20MB/s 累加，封顶 900s），池模式与独立进程模式统一使用
- **4（gpx_utils.py）**：注释/处理指令跳过处消费超过 1MB 即截断已处理前缀（`buf = buf[pos:]`），内存有界且总耗时变为线性
- **5（exif_utils.py）**：stderr 队列只保留最近 2 万行，新增 `_stderr_trimmed` 计数保持序号还原正确（取"本命令应得行数"的队尾切片）
- **6（exif_utils.py）**：编码重试仍失败时区分"文件名非ASCII"与"其它真实错误"（透传 err2 原文）

### 验证结果（全部通过）
- `compileall` 全量编译 ✅
- 问题 1/2 复测：TXT 输出含 `时间: 2024-01-15 10:30:00`、统计含 `时间跨度`；dict 分支回归正常 ✅
- 问题 4 复测：60×3MB 前导 **49.4s → 3.6s**（线性）；DTD/实体拒绝功能不变 ✅
- 问题 5 会计逻辑单测：截断 5001 行后仍能正确还原当前命令 stderr ✅
- 问题 3 公式验证：1GB→111s、4GB→264s、10GB→572s、20GB+→900s 封顶 ✅
- 日期 E2E（池写入链路）`DATE E2E OK` ✅
- GPS 全链路 E2E `E2E OK` ✅

### 遗留观察项（未修复，低优先）
- 扫描时每个音频文件启动一次 exiftool 子进程（`media_scanner.py:155`），音频多时扫描偏慢
- GPX 导出年份过滤 1971–2037（`gpx_utils.py`）：64 位平台本可放宽，属保守设计
- `config.py` 文件名日期模式存在先后依赖（年优先在前），新增模式时需保持顺序
- 修复后 `git status` 新增：export_service.py / exif_utils.py / gpx_utils.py 三个文件的二轮改动（未提交）
