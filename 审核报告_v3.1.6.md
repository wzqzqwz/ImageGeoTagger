# ImageGeoTagger 全面审核报告（2026-08-13）

审核日期：2026-08-13
审核对象：ImageGeoTagger v3.1.6（git HEAD `e981b10`）
审核方式：全量源码通读 + 静态检查 + 隔离环境功能断言 + git 状态核对

---

## 一、结论摘要

| 项目 | 结果 |
|---|---|
| 语法编译 | ✅ `compileall` 全量通过（Python 3.13.12，0 错误） |
| 多语言完整性 | ✅ 6 语言包各 588 键，missing=0 / extra=0，完全一致 |
| 功能断言 | ✅ 39 项核心逻辑断言全部通过（日期解析 / 坐标换算 / GPS 校验 / CSV 注入 / 剪贴板解析 / GPX 解析与 DTD 拒载 / 时间匹配 / 导出） |
| git 卫生 | ✅ 工作区干净；`.gitignore` 正确排除 `exiftool/`、`exiftool.exe`、`ImageGeoTagger_v*.zip`、`window_config.json`、`.workbuddy/`；无二进制产物入库 |
| 依赖导入 | ✅ 隔离 venv 安装 exifread/piexif/defusedxml/Pillow 后，非 GUI 模块正常导入并跑通断言 |
| **阻塞问题** | **无** |
| 本轮新发现 | 1 个 P4 文档笔误（`to_degrees` 注释示例值错误）；若干 P3/P4 非阻塞观察项（见第四节） |

**总体评价**：代码质量高，防御性编程到位——并发安全（全局互斥 + per-file 锁 + 常驻 ExifTool 进程池 + 超时看门狗）、写入安全（同目录原子替换 + 写前备份 + 失败自动恢复）、注入防护（CSV 公式注入、XML 实体膨胀、子进程参数列表化 + 换行字符拒执）均属工业级。无数据损坏/安全级别的缺陷，**可发布**。

---

## 二、验证记录

### 2.1 静态检查
- `python -m compileall -q models services ui utils main.py config.py __main__.py __init__.py` → 退出码 0
- 依赖库受限环境下（managed Python 3.13 无 tkinter/exifread），按隔离规则在 `~/.workbuddy/binaries/python/envs/igt-audit` 建 venv 安装依赖后完成功能验证，未污染用户环境。

### 2.2 功能断言（39 项全过，抽查核心正确性）
| 分组 | 覆盖点 | 结果 |
|---|---|---|
| 日期解析 | 年在前 / 时间在前 / 纯日期 / 截图命名 / 无日期共 10 例 | ✅ 10/10 |
| 坐标换算 | `to_degrees` 度分秒换算及长尾消除 | ✅ |
| GPS 校验 | 合法值 / 边界 / 越界拒绝 / NaN 拒绝 | ✅ 4/4 |
| CSV 注入 | `=` `+` `-` `@` 前缀加引号、正常值原样 | ✅ 5/5 |
| 剪贴板解析 | `?ll=` 编码/未编码、三元组、经纬序自适应 | ✅ 4/4 |
| GPX | trkpt/wpt 解析、海拔、DTD 实体拒载、导出 trkpt 与 UTC 时间 | ✅ 7/7 |
| 时间匹配 | 二分+扩边最近点、阈值外返回 None | ✅ 2/2 |
| 导出 | JSON 计数/坐标取整、CSV 无长尾、TXT 写入 | ✅ 5/5 |

---

## 三、与上一轮（2026-08-12）审核的关系

上一轮已覆盖 `908a0d3`、`034731a` 两个提交并修复 i18n 缺失键；本轮 HEAD `e981b10`（v3.1.6）仅在其后补了「正在导出，请稍候」6 语言翻译并升版本号，**核心代码零变更**。因此上一轮结论对代码主体仍然有效，本轮为全量复核 + 补充新观察项。

---

## 四、发现的问题（按严重度）

### P4-1（本轮新发现）`to_degrees` 注释示例值错误
- **位置**：`utils/exif_utils.py` 第 616 行 docstring
- **现象**：注释写「116.39747° 转换为 ((116,1), (23,1), (1430892,1e6))」，但实际正确输出为 `((116,1),(23,1),(50892000,1000000))`（50.892″×1e6）。代码行为正确，仅注释过期。
- **影响**：无功能影响，误导后续维护者。

### P3-1（沿用上轮，未改）`_show_gpx_stats` 未做时区归一化
- **位置**：`ui/results_window.py` 第 797–800 行
- **现象**：对 `p['datetime']` 直接 `min/max`，未套 `_naive_dt`。当前安全（GPX 解析已统一转本地 naive），但若未来有 aware 时间进入 `gps_data` 会在此抛 `TypeError`。属同类脆弱点，建议补归一化（约 5 行）。

### P3-2（沿用上轮，未改）`_naive_dt` 语义重复定义
- **位置**：`services/export_service.py` 与 `utils/gpx_utils.py` 各定义一份，`geo_processor._to_utc_naive`、`media_scanner._sort_key` 为等价逻辑。
- **建议**：收敛到公共工具模块，避免多处漂移。

### P3-3（沿用上轮，未改）`_paste_location` 空剪贴板记错误日志
- **位置**：`ui/dialogs.py` 第 308–322 行
- **现象**：系统剪贴板为空时 `clipboard_get()` 抛 `tk.TclError`，被外层 `except Exception: log_exc()` 记为错误。功能正常，仅日志噪音，建议 `except tk.TclError: pass` 与轮询处对齐。

### P4-2（本轮新发现）`media_scanner.scan_folder` 未分块提交任务
- **位置**：`services/media_scanner.py` 第 113 行 `futs = {pool.submit(...) for p in files}`
- **现象**：一次性为所有文件创建 Future 对象；而 `date_tab._scan_thread`（第 953–958 行）刻意每 500 个分块提交以控内存峰值。超大目录（10 万+ 文件）时此处会一次性持有全部 Future。低严重度，建议对齐分块策略。

### P4-3（本轮新发现）视频/音频 GPS 写入用原始值、未用校验返回值
- **位置**：`utils/exif_utils.py` `update_video_gps` / `update_audio_gps`
- **现象**：开头调用了 `_validate_location(location_info)` 但丢弃返回值，方法列表直接 `f'...{location_info["latitude"]}...'` 使用原始值（可能为字符串）。当前所有调用方均传入 float，无实际问题；但与 `update_raw_gps`/`_build_gps_exiftool_args`（用校验后的 float）不一致，属健壮性瑕疵。

### P4-4（本轮新发现）GPX 导出对 1971–2037 范围外的点静默丢弃
- **位置**：`utils/gpx_utils.py` `_local_to_utc_str` / `create_gpx_element`
- **现象**：`mktime` 32 位范围限制导致年份 <1971 或 >2037 的点被静默过滤，`<time>` 可能输出空标签。代码注释已说明，但对摄影场景几乎无影响（照片日期不会在 1971 前），仅提示关注。

### P4-5（本轮新发现）文档与实现不一致（日期清除的占位值）
- **位置**：`services/date_processor.py` `clear_file_shooting_date` docstring 写「1970:01:01」，实际 `blank_exif_dates` 写入「0001:01:01」。注释与行为不符，轻微。

### P4-6 依赖未锁定版本 + piexif 停维护
- `requirements.txt` 用 `>=` 范围（`exifread>=3.0.0`、`Pillow>=10`、`piexif>=1.1.3` 等）未锁定；`piexif` 上游已多年停维护。建议后续对发布版固定版本号（或 pip freeze 生成 lock 文件），并评估 piexif 的替代（ExifTool 已是全格式兜底）。

---

## 五、遗留风险（沿用历史报告，长期有效）

1. **跨时区匹配前提**：时间匹配算法基于「相机时区 == 本机时区」假设（`geo_processor._to_utc_naive` 注释已明确）。跨时区拍摄后回国处理会系统性偏移，UI 未提供时区修正选项。
2. **根目录 `exiftool.exe`**：与 `exiftool/` 内为同尺寸 PAR 包装（58 KB，依赖同目录 `.pm/.dll`），已由 `.gitignore` 排除、不入库，但本地目录存在两份，属可清理冗余。

---

## 六、建议（按优先级）

1. 修正 `to_degrees` 注释示例值（1 行，P4-1）。
2. 给 `_show_gpx_stats` 补时区归一化（5 行，P3-1）。
3. `_paste_location` 增加 `except tk.TclError: pass`（P3-3）。
4. `media_scanner` 对齐 `date_tab` 的分块提交策略（P4-2）。
5. 收敛 `_naive_dt` 到公共模块，消除三处重复（P3-2）。
6. 发布版锁定依赖版本，评估 piexif 替代方案（P4-6）。

以上均不阻塞发布，可在后续迭代一并处理。

---

## 七、审核结论

**当前 HEAD（e981b10 / v3.1.6）可发布。** 无阻塞问题、无数据损坏风险、无安全漏洞；静态检查与 39 项功能断言全部通过，多语言与 git 卫生良好。发现的均为 P3/P4 级文档笔误与健壮性观察项，建议按第六节清单在后续迭代处理。

---

## 八、修复记录（2026-08-13 复核后逐项修复）

对第六节清单逐项重新确认后，修复结果如下：

| 编号 | 问题 | 确认结论 | 处理 |
|---|---|---|---|
| P4-1 | `to_degrees` 注释示例值 1430892 错误 | 确认：代码正确、注释过期 | ✅ 已改注释为 50892000 |
| P3-1 | `_show_gpx_stats` 未做时区归一化 | 确认：真实脆弱点 | ✅ 改用 `to_local_naive` 归一化 |
| P3-2 | `_naive_dt` 三处重复定义 | 确认：export_service/gpx_utils/geo_processor 语义一致 | ✅ 收敛到新增 `utils/datetime_utils.py::to_local_naive`，三处引用、media_scanner 加注释 |
| P3-3 | `_paste_location` 空剪贴板误记错误日志 | 确认：真实日志噪音 | ✅ 增加 `except tk.TclError: pass` |
| P4-2 | `media_scanner` 未分块提交 futures | 确认：真实内存隐患 | ✅ 改为每 500 个分块提交（对齐 date_tab） |
| P4-3 | 视频/音频 GPS 写入丢弃校验返回值 | 确认：潜在 TypeError（字符串坐标比较）隐患 | ✅ `update_video_gps`/`update_audio_gps` 改用 `_validate_location` 返回的 lat/lon/alt |
| P4-5 | `clear_file_shooting_date` docstring 1970 vs 0001 | 确认：注释与实现不符 | ✅ 已改为 0001:01:01 |
| P4-4 | GPX 导出 1971–2037 范围外静默丢弃 | 确认：`mktime` 32 位限制下的有意边界保护，代码已注释；照片日期不会越界，改时区转换有引入新 bug 风险 | ⏸️ 不修（设计权衡） |
| P4-6 | 依赖未锁定 + piexif 停维护 | 确认：属发布策略/供应链决策，非代码 bug | ⏸️ 不修（需用户决策版本策略） |

### 修复回归验证
- `compileall` 全量通过（0 错误）
- 隔离环境 31 项功能断言全过（含 `to_local_naive` 收敛后语义、`_to_utc_naive` 委托、混合时区 GPX 导出、media_scanner 1200 文件跨分块边界分类/进度回调、视频/音频 GPS 字符串坐标规范化）
- `git diff` 逐文件核对：改动最小化、无副作用，`_validate_location` 自身未被误改

改动文件：`utils/exif_utils.py`、`services/date_processor.py`、`services/export_service.py`、`services/geo_processor.py`、`services/media_scanner.py`、`utils/gpx_utils.py`、`ui/dialogs.py`、`ui/results_window.py`；新增 `utils/datetime_utils.py`。

