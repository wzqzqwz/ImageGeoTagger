# ImageGeoTagger v3.1.5 全面审核报告

审核日期：2026-08-06
审核范围：全部源码（models / services / ui / utils / config / main / __init__）、多语言资源（6 语言各 587 键）、静态检查、关键函数单元验证（60 项）、GUI 冒烟、打包配置（ImageGeoTagger.spec）、依赖环境、ExifTool 路径解析
前置基础：`审核报告.md`（v3.1.2，2026-08-05）结论全部仍有效，本文为 v3.1.3 → v3.1.5 区间及全项目复验

---

## 一、结论摘要

| 项目 | 结果 |
|---|---|
| 语法编译 | ✅ `python -m compileall` 全量通过 |
| 静态分析 | ✅ `python -m pyflakes` 零告警（无未使用导入/未定义名） |
| 单元/链路验证 | ✅ 60 项全通过（详见第二节），含回归用例 |
| GUI 冒烟 | ✅ MainWindow / ResultsWindow（4 标签页）创建成功 |
| 多语言完整性 | ✅ zh/en/fr/ru/es/ar 各 **587 键**，missing=0、extra=0 |
| 依赖环境 | ✅ exifread 3.5.1、Pillow 12.2.0、piexif 1.1.3、tkinterdnd2、defusedxml 0.7.1（全部满足 requirements） |
| ExifTool | ✅ `get_exiftool_path()` 实测解析到 `exiftool\exiftool.exe`（13.59）并 `-ver` 验证通过；根目录损坏 wrapper 按搜索顺序永远不会被选中 |
| 打包配置 | ✅ spec 递归打包 `exiftool/` 目录、locales、icons；hiddenimports 覆盖 tkinterdnd2/exifread/piexif/PIL |
| 本轮发现并修复 | ⚠️ **1 个中高严重度 bug**：时间在前文件名格式被纯日期模式遮蔽，时间静默丢失（详见第三节） |
| 未提交改动 | `__init__.py`（3.1.4→3.1.5 版本号）、`config.py`（本轮修复）+ 3 个构建产物 zip（未跟踪） |
| **阻塞问题** | **无** |

---

## 二、实测验证记录

### 2.1 静态检查
- `python -m compileall -q models services ui utils main.py config.py __main__.py __init__.py` → COMPILE OK
- `python -m pyflakes ...`（全量）→ 退出码 0，零告警
- i18n 一致性脚本：6 语言 × 587 键全等，无缺失/无多余

### 2.2 文件名日期解析 `parse_datetime_from_filename`（17 用例）
| 输入 | 期望 | 结果 |
|---|---|---|
| `IMG_20240115_103000.jpg` / `.mp4` | 2024-01-15 10:30:00 | ✅ |
| `2024-01-15_10-30-00.png` | 2024-01-15 10:30:00 | ✅ |
| `Screenshot_20240115-103000.png` | 2024-01-15 10:30:00 | ✅ |
| `photo_20240115-103000.png` | 2024-01-15 10:30:00 | ✅ |
| `20240115103000.jpg`（12 位） | 2024-01-15 10:30:00 | ✅ |
| `2024-01-15.jpg` / `20240115.jpg` | 2024-01-15 00:00:00 | ✅ |
| `no_date_here.png` | None | ✅ |
| `150124_153000.jpg`（YYMMDD 前导） | None（防歧义） | ✅ |
| **`10-30-00_2024-01-15.jpg` / `10-30-00 2024-01-15.jpg`** | 10:30:00 | ✅ **修复后** |
| **`103000_20240115.jpg` / `103000_20240115.png`** | 10:30:00 | ✅ **修复后** |
| `150124_20240115.jpg`（歧义前导） | 2024-01-15 15:01:24（按时间解读，UI 已声明） | ✅ |
| `12345678901234_abc.jpg` | None | ✅ |
| 另 8 项原有格式回归 | 与原行为一致 | ✅ 零回归 |

### 2.3 坐标与 EXIF 工具（17 用例）
- `to_degrees` 往返精度：8 个样本（含 0、±180、90、1e-6）误差均 < 1e-9 ✅
- `_parse_video_time_detailed`：`...Z` → UTC 转本地 18:30:00（had_tz=True）；`+08:00` → 10:30:00 不变；裸时间 → 10:30:00（had_tz=False）；毫秒+Z → 正确 ✅
- `format_gps_coord`：`35.420289999999994 → "35.42029"`、负值 `-73.98570000000001 → "-73.9857"`、None → `""` ✅
- `csv_safe`：`= + - @ \t` 前缀全部被 `'` 中和，普通值不变 ✅
- `_find_closest_by_time`：精确边界命中、阈值内命中、超出阈值返回 None、乱序参考列表安全 ✅

### 2.4 GPX 安全与解析（7 用例）
- `_reject_unsafe_xml`：DOCTYPE 拒绝 ✅、裸 ENTITY 拒绝 ✅、正常 GPX 放行 ✅、3MB 注释放行且耗时仅 **0.09s** ✅、>4MB 单注释拒绝 ✅
- `parse_gpx_file`：正常文件解析 1 点 ✅；非法 lat=999 点位被跳过 ✅
- 导出 GPX 再解析往返成功 ✅

### 2.5 导出链路（7 用例）
- TXT：轨迹时间行、时间跨度统计 ✅
- CSV：UTF-8 BOM ✅；长尾小数已消除（含 `35.42029` 而非 `35.420289999999994`）✅
- JSON：gps 轨迹点 datetime 已字符串化 ✅
- 统计：处理文件总数 / 新增位置信息计数正确 ✅

### 2.6 其他（3 用例）
- `_no_clobber_rename`：NT 分支正常、目标=自身安全 ✅
- `log_exc` 返回完整 traceback ✅
- GUI 冒烟：`TkinterDnD.Tk()` + MainWindow + ResultsWindow（4 标签页：有位置/无位置/GPX/统计）✅

---

## 三、本轮发现并修复的问题

### P1（已修复）时间在前文件名格式被纯日期模式遮蔽，时间静默丢失
- **现象**：`10-30-00_2024-01-15.jpg`、`10-30-00 2024-01-15.jpg`、`103000_20240115.jpg` 等"时间在前"文件名仅解析出日期 `2024-01-15 00:00:00`，**时分秒被静默丢弃**。若用户以此格式文件设置拍摄时间，会写入错误时间（00:00:00）。
- **根因**：`config.py` `DATETIME_PATTERNS` 中纯日期模式（`YYYY-MM-DD`、`YYYYMMDD`）位于"时间在前"模式（`HH-MM-SS_YYYY-MM-DD`、`HHMMSS_YYYYMMDD`）**之前**；正则对文件名做子串搜索，日期部分总是先被纯日期模式命中 → 时间在前模式成为死代码。
- **影响面**：`ui/date_tab.py` 帮助文本明确宣传这两种"时间在前"格式受支持；`DATETIME_PATTERNS` 注释也声称其生效 → 文档/实际行为不一致。
- **修复**：将两个"时间在前"模式前移到纯日期模式之前（config.py）。纯日期仍作为兜底（`2024-01-15.jpg` 等不受影响）。
- **验证**：修复后 4 个时间在前用例正确解析；原有 10 项格式（年在前/截图/12 位/纯日期/无日期/歧义防护）回归全过。
- **结论勘误**：v3.1.2 报告 2.1 节"时间在前等旧格式均正常"表述不准确，本次实测证明该缺陷自 v3.1.2 之前即存在（`config.py` 历史中时间在前模式从未生效），现已在 v3.1.5 修复。

---

## 四、遗留风险与已知限制（非阻塞）

1. **跨时区轨迹匹配**（沿用上轮）：`geo_processor._to_utc_naive` 依赖系统时区；系统时钟偏移 > 0 时按本地时区处理。使用前提：系统时区与拍摄地一致。
2. **视频时区语义**：带 `Z`/偏移的视频时间转本地时区（如 `Z → +08:00 → 18:30:00`），无时区视频按本地时间解读；行为有文档可预期性，但 UTC 视频在不一致时区下会偏移。
3. **歧义格式**：`150124_20240115` 之类前导 6 位按"时间"解读（UI 帮助已声明）；纯日期 8 位、12 位格式无歧义。
4. **根目录 `exiftool.exe` 为损坏 wrapper**：存在但无害——`get_exiftool_path()` 实测搜索顺序 `app_dir\exiftool\`（13.59）优先，wrapper 永不命中；建议后续清理删除避免误用。
5. 未跟踪构建产物：`ImageGeoTagger_v3.1.4_src.zip`、`ImageGeoTagger_v3.1.4_win64.zip`、`ImageGeoTagger_v3.1.5_win64.zip`（如需入库请先确认 .gitignore 策略）。

---

## 五、与 v3.1.2 报告差异

| 项目 | v3.1.2（2026-08-05） | v3.1.5（本轮） |
|---|---|---|
| 版本 | 3.1.2 | 3.1.4→3.1.5（`__init__.py`，未提交） |
| i18n 键数 | 571 | **587**（6 语言一致，本轮复核） |
| 静态分析 | compileall | compileall + **pyflakes 零告警** |
| 单元验证 | 6 项链路 | **60 项断言**（含回归） |
| 新发现缺陷 | — | **时间在前格式遮蔽（已修复）** |
| v3.1.2 结论 | 全部仍有效 | 全部仍有效，1 项勘误（见第三节） |

---

## 六、审核结论

ImageGeoTagger v3.1.5 代码质量良好：静态检查零告警、60 项单元验证全过、GUI 冒烟正常、多语言完整、依赖与打包配置正确。本轮修复 1 个中高严重度的时间解析缺陷（静默错误时间写入风险），无阻塞问题，**可发布**。建议后续：清理根目录损坏 wrapper、将 3 个 zip 产物纳入版本管理策略。
