# ImageGeoTagger 跨平台兼容性审查报告

审查日期：2026-08-13
审查对象：HEAD `8ebb5be`（v3.1.7）+ 工作区未提交修复
审查方式：全量源码通读 + 平台特定 API/subprocess/路径/编码专项排查 + 静态检查 + 断言

---

## 一、结论摘要

| 项目 | 结果 |
|---|---|
| 语法编译 | ✅ 全量通过 |
| 多语言完整性 | ✅ 6×588 键一致 |
| 路径处理 | ✅ 全部 `os.path.join`，无硬编码分隔符 |
| 平台 API 保护 | ✅ ctypes/os.startfile/osascript/gio 均有平台判断 + try/except |
| 编码处理 | ✅ `os.name` 区分 Windows 代码页 vs UTF-8 |
| 文件操作 | ✅ rename/link 按平台分支 |
| 回收站 | ✅ Windows/macOS/Linux 三平台实现 |
| 打包 | ✅ spec 按平台选图标/UPX |
| **阻塞问题** | **无** |
| 本轮修复 | 3 处跨平台相关改进 |

**总体评价**：代码跨平台基础扎实，绝大多数平台差异点都有正确的分支与兜底。本轮针对审查发现的 3 处跨平台问题做了修复，无阻塞缺陷，可支撑 Windows/macOS/Linux 三平台运行。

---

## 二、本轮修复（3 处）

### 2.1 「显示文件位置」含空格路径失效（`utils/platform_utils.py`，上轮已修复）
- 根因：`'/select,' + 路径` 合并成单参数，含空格路径被 explorer 截断。
- 修复：改为 `'/select,', 路径` 分离传参。已真机实测通过（见第 6 轮报告）。

### 2.2 macOS 系统语言无法自动检测（`utils/i18n.py`，本轮新增）
- 根因：macOS GUI 应用通常无 `LANG` 环境变量、`locale` 未初始化，原逻辑回退到 `'en'`，中文用户界面变英文。
- 修复：新增 `darwin` 分支，用 `defaults read -g AppleLanguages` 读取系统首选语言（BCP-47 前缀映射到 `zh/en/fr/ru/es/ar`）。
- 验证：`zh-Hans-CN → zh`、`ru-RU → ru` 解析正确；异常/超时静默回退。

### 2.3 回收站 subprocess 编码未显式指定（`utils/recycle_bin.py`，本轮新增）
- 根因：macOS `osascript`、Linux `gio`/`trash-put` 的 `subprocess.run` 未指定 `encoding`，非 UTF-8 locale 下中文输出可能乱码。
- 修复：三处调用补 `encoding='utf-8'`（macOS/Linux 均为 UTF-8 输出）。

---

## 三、审查确认良好（跨平台维度逐项核查）

| 维度 | 实现 | 结论 |
|---|---|---|
| 路径拼接 | 全程 `os.path.join`；唯一硬编码反斜杠在 `config.py` 的 Windows ExifTool 路径（仅 Windows 使用） | ✅ |
| 扩展名比较 | 统一 `.lower()`，避免大小写敏感差异 | ✅ |
| Windows API | `ctypes.windll`（GetACP/GetUserDefaultUILanguage/GetSystemMetrics/SHFileOperation）均有 `os.name=='nt'` 或 try/except 保护 | ✅ |
| 打开文件 | `os.startfile`(Win) / `open`(mac) / `xdg-open`(Linux) 分支 | ✅ |
| 定位文件 | `explorer /select`(Win) / `open -R`(mac) / `xdg-open 目录`(Linux)，已修空格路径 | ✅ |
| 创建时间 | `st_birthtime`(mac/BSD) / `st_ctime`(Win) / Linux 返回 None 并回退修改时间 | ✅ |
| 回收站 | SHFileOperation(Win) / osascript(mac) / gio·trash-put(Linux) | ✅ |
| 原子重命名 | `os.rename`(Win) / `os.link+unlink`(POSIX，含跨分区回退) | ✅ |
| 文件名非法字符 | 三平台分别校验（Win `< > : " / \ | ? *`，mac `:` `/`，Linux `/`） | ✅ |
| ExifTool 查找 | 平台分支：`exiftool.exe` vs `exiftool` + 各平台路径 + `os.access(X_OK)` | ✅ |
| 编码 | `_system_codepage()` Windows 返回 ACP、其余 65001；subprocess 传参用 Unicode API | ✅ |
| 打包 spec | 按平台选 `.ico`/`.icns`/`.png`，UPX 检测，exiftool 目录递归 | ✅ |
| 图标加载 | `iconbitmap(.ico)`(Win) + `iconphoto(.png)`(跨平台) | ✅ |

---

## 四、依赖与打包说明（已核实）

1. **打包版 exe 自带全部 GUI 依赖，最终用户零依赖**：
   - 实测 `dist/ImageGeoTagger.exe` 内含 `tcl86t.dll`、`tk86t.dll`（Tcl/Tk 运行时）及 `tkinterdnd2\tkdnd\win-x64\libtkdnd2.10.1.dll` 等拖放库与 tcl 脚本（PyInstaller hook 自动收集）。
   - 因此下载 exe 的用户**无需安装 python-tk 或 tkdnd**。
2. **源码运行（开发者）**：
   - `tkinter` 需 `python-tk`（Windows 的 python.org 安装包默认自带；macOS `brew install python-tk`、Linux `apt install python3-tk`，属系统级依赖、pip 无法安装）。
   - `tkinterdnd2`（当前 0.6.2）的 wheel 已内置各平台 tkdnd 二进制，`pip install -r requirements.txt` 即可，**无需额外安装 tkdnd**（此前报告所述「需额外安装 tkdnd」为过时信息，已更正）。
3. **ExifTool 平台版本**：打包时需将 `exiftool/` 目录替换为目标平台的 ExifTool（README 已说明）。
4. **跨时区匹配前提**：时间匹配基于「相机时区 == 本机时区」假设（历史遗留，非跨平台问题）。

---

## 五、建议

1. 提交本轮 3 处修复（建议版本号升至 v3.1.8）。
2. 在 macOS/Linux 真机各做一次冒烟测试（启动、扫描、GPS 写入、回收站、显示文件位置、语言切换）。
3. 发布版固定依赖版本（历史遗留项）。

---

## 六、结论

**代码跨平台兼容性良好，无阻塞缺陷。** 本轮修复了 3 处跨平台问题（含空格路径定位、macOS 语言检测、回收站编码），其余平台差异点均有正确分支与兜底。可在 Windows/macOS/Linux 三平台运行，建议提交后分别在 macOS/Linux 真机冒烟验证。
