# ImageGeoTagger 全面审核报告（第 6 轮）

审核日期：2026-08-13
审核对象：HEAD `8ebb5be`（v3.1.7）+ 1 处未提交修复（`utils/platform_utils.py`）
审核方式：全量源码通读 + subprocess 调用点专项排查 + 静态检查 + 功能断言 + 真机实测

---

## 一、结论摘要

| 项目 | 结果 |
|---|---|
| 语法编译 | ✅ `compileall` 全量通过（0 错误） |
| 多语言完整性 | ✅ 6 语言包各 588 键，完全一致 |
| 功能断言 | ✅ 核心逻辑 6 项 + 修复回归 4 项全部通过 |
| 真机实测 | ✅ 修复后含空格路径文件可正确定位到所在文件夹 |
| subprocess 排查 | ✅ 全部列表传参，无其它含空格路径拼接隐患 |
| **阻塞问题** | **无** |
| 本轮修复 | 1 处（「显示文件位置」含空格路径失效，**未提交**） |

**总体评价**：本轮针对用户报告的「显示文件位置」失效问题，定位到 `show_file_in_explorer` 中 `explorer /select,` 参数拼接 bug 并修复。全量排查确认这是唯一含空格路径隐患，其余 subprocess 调用均为列表传参（安全）。无新增阻塞问题。

---

## 二、本轮修复：`show_file_in_explorer` 含空格路径定位失效

### 2.1 现象
在「处理结果详情」窗口右键「显示文件位置」：
- 根目录文件 `DSC_9505.NEF`（路径无空格）→ 能正确定位 ✅
- Lightroom Smart Preview 文件 `86960FE8-....dng`（路径含 `Lrc Smart Previews.lrdata` 空格）→ 定位失败，explorer 回退到「文档」❌

### 2.2 根因
原代码：
```python
subprocess.run(['explorer', '/select,' + abs_path], check=False, timeout=5)
```
`/select,` 与路径被拼接成**单个参数**，`list2cmdline` 生成 `explorer "/select,D:\...\Lrc Smart Previews.lrdata\..."`（引号包裹整个参数）。explorer 解析时把含空格路径**截断**（只取到空格前一段），定位失败回退默认位置。

### 2.3 修复
```python
subprocess.run(['explorer', '/select,', abs_path], check=False, timeout=5)
```
`/select,` 与路径分离传参，`list2cmdline` 生成 `explorer /select, "D:\...\Lrc Smart Previews.lrdata\..."`（路径单独加引号），explorer 正确解析完整路径。同时修正原代码中「必须合并」的错误注释。

### 2.4 验证
- 真机实测：修复后定位该 dng，explorer 窗口正确打开「8696 - 文件资源管理器」；
- 断言：含空格路径被正确引号包裹、`/select,` 与路径分离、无合并写法残留，4/4 通过。

---

## 三、subprocess 调用点专项排查

针对同类「含空格路径字符串拼接」隐患，全量排查所有子进程/打开调用：

| 位置 | 调用 | 传参形式 | 结论 |
|---|---|---|---|
| `exif_utils.py` | `Popen`/`subprocess.run`（ExifTool） | 列表（`[tool, *args, file_path]`） | ✅ 安全 |
| `platform_utils.open_file_with_system` | `os.startfile` | 直接传路径（ShellExecute） | ✅ 安全 |
| `platform_utils.show_file_in_explorer` | `explorer /select,` | 已修复为分离传参 | ✅ 已修复 |
| `recycle_bin.py` | `subprocess.run`（gio/trash-put/osascript） | 列表 | ✅ 安全 |

**结论**：`show_file_in_explorer` 是唯一使用「字符串拼接参数」的调用，其余均列表传参，无同类隐患。

---

## 四、验证记录

- `python -m compileall -q models services ui utils main.py config.py __main__.py __init__.py` → 退出码 0
- 多语言键一致性：zh/en/fr/ru/es/ar 各 588 键，missing=0 / extra=0
- 功能断言：`to_local_naive` 归一化、`csv_safe` 注入防护、`to_degrees` 坐标换算、文件名日期解析 4 项核心逻辑 + `show_file_in_explorer` 修复回归 4 项，全部通过
- 真机实测（Windows explorer）：修复后含空格路径正确定位

---

## 五、当前状态与建议

**当前状态**：修复仅存在于工作区（`git status` 显示 `M utils/platform_utils.py`），**尚未提交**。HEAD 仍为 `8ebb5be`（v3.1.7）。

**建议**：
1. 提交本次修复（建议版本号升至 3.1.8，或并入后续发布）。
2. 如需让已打包的 `dist/ImageGeoTagger.exe` 生效，需重新打包。

---

## 六、审核结论

**代码主体可发布。** 本轮发现的「显示文件位置」含空格路径失效 bug 已定位并修复，真机实测与断言均通过；全量排查确认无同类隐患，无新增阻塞问题。仅需按第五节提交修复（及可选重新打包）。
