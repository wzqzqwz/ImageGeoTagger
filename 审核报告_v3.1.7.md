# ImageGeoTagger 全面审核报告（第 5 轮）

审核日期：2026-08-13
审核对象：工作区当前状态（HEAD `e981b10` / v3.1.6 + 第 4 轮修复未提交的 8 个修改文件 + 2 个新增文件）
审核方式：全量源码通读（29 个 Python 文件 / 10223 行）+ 静态检查 + 隔离环境功能断言 + git 状态核对

---

## 一、结论摘要

| 项目 | 结果 |
|---|---|
| 语法编译 | ✅ `compileall` 全量通过（Python 3.13.12，0 错误） |
| 多语言完整性 | ✅ 6 语言包各 588 键，missing=0 / extra=0，完全一致 |
| 功能断言 | ✅ 31 项核心逻辑断言全部通过（含第 4 轮修复的全部回归点） |
| git 卫生 | ✅ 57 个跟踪文件，无二进制产物（exe/zip/pyc）入库 |
| 第 4 轮 7 项修复 | ✅ 逐项核对已正确落地，回归验证通过 |
| **阻塞问题** | **无** |
| **本轮关键发现** | ⚠️ **第 4 轮修复仅存在于工作区，尚未提交/推送**（见第四节） |

**总体评价**：代码质量保持工业级水准。第 4 轮审核提出的 7 项 P3/P4 修复（时区归一化收敛、分块提交、剪贴板 TclError、字符串坐标规范化等）已全部正确落实，且经回归断言验证无副作用。无新增数据损坏/安全级别缺陷，代码主体可发布。**唯一待办是把已完成的修复提交并推送，否则远端仓库仍停留在未修复的 v3.1.6。**

---

## 二、验证记录

### 2.1 静态检查
- `python -m compileall -q models services ui utils main.py config.py __main__.py __init__.py` → 退出码 0
- 多语言键一致性：zh/en/fr/ru/es/ar 各 588 键，两两无缺键、无冗余键。

### 2.2 功能断言（31 项全过，重点覆盖第 4 轮修复回归点）
| 分组 | 覆盖点 | 结果 |
|---|---|---|
| `to_local_naive` 归一化 | None 透传 / naive 原样 / aware 转本地 naive / 往返自洽 | ✅ 4/4 |
| 坐标换算 | `to_degrees` 116.39747°→50892000/1e6（注释已修正）、0° 边界 | ✅ 4/4 |
| GPS 校验 | 字符串坐标规范化 float / 类型 / 越界拒绝 / NaN 拒绝 / 无高度返回 None | ✅ 6/6 |
| CSV 注入 | `=` `+` `-` `@` 前缀加引号、正常值原样 | ✅ 5/5 |
| 剪贴板解析 | `?ll=` 编码/未编码、三元组、纯两数、经纬序自适应、无坐标返回 None | ✅ 6/6 |
| GPX | trkpt/wpt 共 3 点、海拔、时间转本地 naive、DTD 实体拒载、小写 z | ✅ 5/5 |
| 时间匹配 | 二分最近点、阈值外返回 None | ✅ 2/2 |
| 导出 | JSON 计数/坐标取整、CSV UTF-8 BOM（原始字节 `EF BB BF` 验证）、统计 | ✅ 4/4 |
| 分块扫描 | 1200 文件跨分块边界全部扫到、分类正确 | ✅ 2/2 |

> 注：隔离 venv 无 tkinter，剪贴板解析用例通过向 `sys.modules` 注入假 `tkinter`/`tkinter.ttk` 模块后测纯逻辑函数（沿用历史方法）。

---

## 三、第 4 轮修复回归确认

对第 4 轮（`审核报告_v3.1.6.md` 第八节）7 项修复逐项复核：

| 编号 | 修复项 | 复核结果 |
|---|---|---|
| P4-1 | `to_degrees` 注释 1430892→50892000 | ✅ 已改，断言验证 116.39747° 输出秒=50892000 |
| P3-1 | `_show_gpx_stats` 时区归一化 | ✅ 改用 `to_local_naive` |
| P3-2 | `_naive_dt` 三处收敛到 `datetime_utils.to_local_naive` | ✅ export_service/gpx_utils/geo_processor 三处引用，media_scanner 加注释保留 `_sort_key` 特殊语义 |
| P3-3 | `_paste_location` 空剪贴板 `except tk.TclError: pass` | ✅ 已加 |
| P4-2 | `media_scanner.scan_folder` 分块提交 | ✅ 每 500 个分块，1200 文件跨边界验证通过 |
| P4-3 | 视频/音频 GPS 用 `_validate_location` 返回值 | ✅ 改用返回的 lat/lon/alt，字符串坐标规范化断言通过 |
| P4-5 | `clear_file_shooting_date` docstring 1970→0001 | ✅ 已改 |

`git diff` 逐文件核对：改动最小化，`_validate_location` 自身未被误改，`update_video_gps`/`update_audio_gps` 行为与 `update_raw_gps`/`_build_gps_exiftool_args` 对齐。

---

## 四、本轮关键发现

### ⚠️ P2-1（流程）第 4 轮修复未提交、未推送
- **现象**：`git status` 显示 8 个修改文件 + 2 个未跟踪文件，HEAD 仍停在 `e981b10`（v3.1.6）。
  - 修改：`services/date_processor.py`、`services/export_service.py`、`services/geo_processor.py`、`services/media_scanner.py`、`ui/dialogs.py`、`ui/results_window.py`、`utils/exif_utils.py`、`utils/gpx_utils.py`
  - 新增：`utils/datetime_utils.py`、`审核报告_v3.1.6.md`
- **影响**：从 GitHub 拉取 HEAD `e981b10` 得到的仍是**未修复**的 v3.1.6——第 4 轮确认的 7 项缺陷（含字符串坐标规范化、分块提交等健壮性修复）对远端用户不可见；本地工作区一旦丢失，修复将随之丢失。
- **建议**：`git add` 上述 10 个文件 → 提交（建议版本号 3.1.5→3.1.7）→ 推送 `origin/master`。`utils/datetime_utils.py` 是新增模块，务必随提交一并入库。

---

## 五、本轮新发现（P4 观察项，均不阻塞）

### P4-1 `create_gpx_element` 注释精度与代码不符
- **位置**：`utils/gpx_utils.py` 第 355 行
- **现象**：注释写「6 位小数与手机照片 EXIF 精度一致（1e-6 度 ≈ 11 厘米）」，但代码用 `:.8f`（8 位小数 ≈ 1e-8 度 ≈ 1 毫米），与 `format_gps_coord` 的 8 位精度一致。仅注释过期，建议改为「8 位小数」。

### P4-2 `update_audio_gps` 高度补写注释缩进错位
- **位置**：`utils/exif_utils.py` 第 1248 行
- **现象**：`# 高度独立补写…` 注释缩进 4 空格，其下 `if success and alt is not None:` 缩进 8 空格（同函数 `update_video_gps` 第 1184 行对应注释为 8 空格）。纯排版瑕疵，不影响逻辑（`if` 正确嵌套于 try/finally 内）。

### P4-3 `log_exc` 冗余判断 + `setup_logging` 微小竞态
- **位置**：`utils/logging_utils.py` 第 83 行 `if logger.handlers: pass else: setup_logging()`
- **现象**：写法别扭（`if...pass else...`）。另 `setup_logging` 在锁内置 `_initialized=True`、在锁外添加 handler，存在极小竞态窗口：并发首次调用可能读到 `_initialized=True` 但 handler 尚未添加，导致该条 `log_exc` 写入无 handler 的 logger 而丢失。实际影响可忽略（`main()` 在派发线程前已调用 `setup_logging`），建议后续将 handler 添加移入锁内。

### P4-4 `_detect_system_lang` 大小写处理不一致
- **位置**：`utils/i18n.py` 第 43 行
- **现象**：Windows 分支 `name.split('_')[0]` 未 `.lower()`，而第 32、51 行分支均 `.lower()`。当前 `locale.windows_locale` 值本身为小写（如 `zh_CN`）故无实际问题，但属不一致，建议补 `.lower()` 防未来数据源变化。

---

## 六、遗留风险（沿用历史报告，长期有效）

1. **跨时区匹配前提**：时间匹配基于「相机时区 == 本机时区」假设（`geo_processor._to_utc_naive` 注释已明确）。跨时区拍摄后回国处理会系统性偏移，UI 未提供时区修正选项。
2. **根目录 `exiftool.exe`**：与 `exiftool/` 内为同尺寸 PAR 包装，已由 `.gitignore` 排除、不入库，但本地目录存在两份，属可清理冗余。
3. **依赖未锁定**：`requirements.txt` 用 `>=` 范围；`piexif` 上游已多年停维护（第 4 轮已评估，属发布策略决策，不修）。

---

## 七、建议（按优先级）

1. **提交并推送第 4 轮修复**（P2-1，10 个文件，建议升版本号至 3.1.7）。
2. 修正 `create_gpx_element` 注释「6 位→8 位」（P4-1，1 行）。
3. `update_audio_gps` 注释缩进对齐（P4-2，1 行）。
4. `log_exc` 简化写法、`setup_logging` handler 移入锁内（P4-3，约 5 行）。
5. `_detect_system_lang` Windows 分支补 `.lower()`（P4-4，1 行）。

以上除 P2-1 外均不阻塞发布，可在后续迭代一并处理。

---

## 八、审核结论

**代码主体可发布。** 第 4 轮修复全部正确落地并经回归断言验证，无阻塞问题、无数据损坏风险、无安全漏洞；静态检查与 31 项功能断言全部通过，多语言与 git 卫生良好。

**唯一必须立即处理的是 P2-1**：第 4 轮修复仅存在于本地工作区、尚未提交推送，远端仓库仍停留在未修复的 v3.1.6。建议在发布下一版前完成提交。
