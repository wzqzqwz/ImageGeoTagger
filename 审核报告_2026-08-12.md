# ImageGeoTagger 全面审核报告（2026-08-12）

审核日期：2026-08-12
审核范围：全项目复检 + 重点审查 v3.1.5 审核（185655f）之后的 2 个未审核提交：
- `908a0d3` 模式切换守卫还原、导出/同位置扫描移入 worker 线程、统计时区归一化、批量操作扫描守卫、txt 导出 source_file 空值与 GPX 混合时区排序崩溃修复、剪贴板空 TclError 静默
- `034731a` 剪贴板 TclError 静默修复位置纠正（误置于外层 except，移回轮询处）

涉及变更文件：`services/export_service.py`、`utils/gpx_utils.py`、`ui/date_tab.py`、`ui/geo_tab.py`、`ui/dialogs.py`（+254/-109 行）。其余文件（models/、services 其余、utils 其余、config.py、main.py、locales/）自 v3.1.5 审核以来零变更，沿用前报告结论。

---

## 一、结论摘要

| 项目 | 结果 |
|---|---|
| 语法编译 | ✅ `compileall` 全量通过（Python 3.13.14） |
| 静态分析 | ✅ `pyflakes` 全量零告警 |
| 变更代码交叉核对 | ✅ `_btn_export`/`register_thread`/`post_to_ui`/`app.lock`/`acquire_processing`/`_dialog_alive` 等调用点与定义全部匹配 |
| 功能验证 | ✅ 12 项断言全过（见第二节） |
| 多语言完整性 | ⚠️→✅ 发现并修复：新键「正在导出，请稍候」6 语言全缺失（已补齐，现 6×588 键一致） |
| 本轮新发现缺陷 | 1 个低严重度（i18n 键缺失，已修复）；3 个非阻塞观察项（见第三节） |
| **阻塞问题** | **无** |

---

## 二、实测验证记录

### 2.1 静态检查
- `python -m compileall -q models services ui utils main.py config.py __main__.py __init__.py` → 通过
- `pyflakes` 全量（27 个 .py）→ 退出码 0，零告警

### 2.2 未审核提交的功能验证（12 项断言）
| # | 验证项 | 结果 |
|---|---|---|
| 1 | `export_service._naive_dt`：aware(UTC) → 本地 naive（10:30Z → 18:30） | ✅ |
| 2 | `_naive_dt`：naive 原样返回、None 原样返回 | ✅ |
| 3 | 混合 aware/naive 列表 `min/max` 不再抛 TypeError | ✅ |
| 4 | `generate_statistics` 混合时区 GPS 轨迹统计正常输出 | ✅ |
| 5 | `create_gpx_element` 混合时区排序不崩溃 | ✅ |
| 6 | GPX 输出 2 个 trkpt，时间正确转 UTC（18:30+08:00 → 10:30Z） | ✅ |
| 7 | `export_to_txt` 轨迹点 `source_file=None` 不再崩溃，输出「未知」 | ✅ |
| 8-12 | `parse_datetime_from_filename` 回归：年在先/时间在前×2/纯日期/无日期 | ✅ |

### 2.3 变更代码审查要点（人工交叉核对）
- **导出 worker 化（date_tab/geo_tab）**：快照在 `app.lock` 下拷贝，导出期间后台任务改列表不影响本次导出一致性；`_exporting` 标志 + 按钮禁用防重入；异常经 `post_to_ui` 回主线程弹窗；`finally` 恢复按钮状态时尊重 `_processing`/`_scanning`/`is_thread_running()`，不覆盖其他任务状态。✅
- **模式切换守卫（date_tab）**：`_applied_mode` 记录上次生效模式，被 `_processing`/`_scanning` 守卫拦截时还原变量，解决 tkinter Radiobutton 先改写变量导致 UI 与数据错位的问题；扫描开始时 `_set_ui_processing_state(True)` 禁用控件、结束恢复。✅
- **同位置扫描 worker 化（dialogs）**：扫描期间用户关闭对话框 → `_dialog_alive()` 检查只释放互斥锁不弹窗；重复点击保存被 `acquire_processing()` 互斥拦截；写盘前 `os.path.exists` 预检防外部改名写错文件。✅
- **`_paste_location` 的 `return` 位置修正**：系统剪贴板有内容（无论能否解析出坐标）时不再落入应用内剪贴板兜底，逻辑正确。✅

---

## 三、本轮发现

### P3（已修复）新增提示语「正在导出，请稍候」未入 6 个语言包
- **现象**：`908a0d3` 在 date_tab/geo_tab 引入 `_('正在导出，请稍候')`，但 locales 6 个文件均未收录；`_()` 回退返回原文键，非中文用户会看到中文提示。
- **修复**：已补齐 zh/en/fr/ru/es/ar 六语言翻译，6×588 键恢复一致（missing=0、extra=0），改动为每文件追加 1 行，diff 最小化。

### 非阻塞观察项（建议后续处理，本轮未改动）
1. **`results_window._show_gpx_stats`（约 797-804 行）仍对原始 datetime 直接 `min/max`**：与本轮修复的统计时区归一化是同一类隐患，此处未同步加 `_naive_dt`。当前安全（GPX 解析已统一转本地 naive），但属脆弱点——未来若有 aware 时间进入 `gps_data` 会在该处崩溃。
2. **`dialogs._paste_location` 空剪贴板仍记错误日志**：`clipboard_get()` 对空剪贴板抛 `tk.TclError`，被外层 `except Exception: log_exc()` 当作错误记录，与本轮修复的轮询处（TclError 静默）不一致；功能正常，仅日志噪音。
3. **`_naive_dt` 在 export_service 与 gpx_utils 各定义一份**（语义一致）：建议后续收敛到公共工具模块，避免三处（含 geo_processor/media_scanner 同类逻辑）漂移。

### 环境说明
- 本轮 GUI 冒烟未重跑：managed Python 3.13 无 tkinter，系统 Python 3.12 缺 defusedxml（按隔离规则未污染用户环境安装）。变更的 UI 代码已通过 pyflakes + 属性/调用点交叉核对覆盖。v3.1.5 报告的 GUI 冒烟结论对未变更部分仍有效。
- v3.1.5 报告第四节遗留风险（根目录损坏 exiftool.exe wrapper、未跟踪 zip 产物、跨时区轨迹匹配前提）仍然适用。

---

## 四、审核结论

两个未审核提交质量良好：worker 线程化与守卫逻辑设计周密（快照一致性、防重入、对话框存活检查、互斥锁兜底），实测无崩溃、无回归。本轮发现 1 个低严重度 i18n 完整性问题并已修复；3 个观察项均不阻塞发布。**当前 HEAD（034731a）可发布**。

建议后续：
1. 给 `_show_gpx_stats` 补 `_naive_dt` 归一化（5 行改动，消除脆弱点）；
2. `_paste_location` 增加 `except tk.TclError: pass` 与轮询处对齐；
3. 收敛 `_naive_dt` 到公共模块；
4. 清理根目录损坏的 `exiftool.exe` wrapper。
